jdk:
  - openjdk17
env:
  RELEASE_VERSION: "1.17.8.1"
before_install:
  - RELEASE_VERSION="$RELEASE_VERSION" ./scripts/prepare-jitpack-artifact.sh
install:
  - cd android && ./gradlew ffmpeg-kit-android-lib:publishBarPublicationToMavenLocal
